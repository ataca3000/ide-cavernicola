{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd lab && npm install && npm run build",
  "outputDirectory": "lab/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
